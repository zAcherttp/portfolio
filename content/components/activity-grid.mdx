## Usage

Supply column data and render each SVG cell yourself. Pointer geometry and active-cell anchors stay inside the primitive.

```tsx title="activity-grid.tsx"
import { ActivityGrid } from "@/components/registry/activity-grid";

<ActivityGrid
  columns={weeks}
  cellSize={8}
  gap={2}
  getKey={(item, column, row) => item?.date ?? `${column}-${row}`}
  renderCell={({ item, columnIndex, rowIndex }) => (
    <rect
      data-date={item?.date}
      height={8}
      width={8}
      x={columnIndex * 10}
      y={rowIndex * 10}
    />
  )}
/>
```

## API reference

<AutoTypeTable path="components/registry/activity-grid.tsx" name="ActivityGridOwnProps" />

## Credits

- Extracted from the portfolio contribution calendar.

## References

- [SVG viewBox](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Attribute/viewBox)
- [Pointer events](https://developer.mozilla.org/en-US/docs/Web/API/Pointer_events)
