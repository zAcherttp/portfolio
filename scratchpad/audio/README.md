# Audio scratchpad

`keyboard-thock-voice-lab.html` is the standalone, dependency-free tuning lab
used to shape the procedural key-down and key-up voices in
`utils/playThockSound.ts`.

Open the HTML file directly in a browser. It embeds its recorded Up reference,
so no server or additional audio file is required.
